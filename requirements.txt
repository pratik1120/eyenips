taichi>=1.7.4     # 1.7.4 is the first wheel built for Python 3.13
numpy>=1.21
# audio capture (optional but recommended). If any are missing the app still
# runs - audio just stays "off".
soundcard>=0.4.3     # system-loopback + mic capture on Windows (WASAPI)
soundfile>=0.12      # decode audio files
sounddevice>=0.4.6   # play audio files
imageio-ffmpeg>=0.5  # bundles the ffmpeg binary used for MP4 export
pillow>=9.0          # draws the live visual inside the app window
opencv-python>=4.5   # camera / image / video input (optional; media stays off without it)
mediapipe>=0.10.30   # person segmentation for "Effect behind subject" (optional; the
                     #   feature just stays off without it). Uses selfie_segmenter.tflite.
mido>=1.3            # MIDI input (optional; the MIDI panel stays "unavailable" without it)
pygame>=2.6          # MIDI backend (PortMidi) — prebuilt wheels everywhere, no compiler
# python-rtmidi      # optional lower-latency MIDI backend; needs a C++ toolchain to build
